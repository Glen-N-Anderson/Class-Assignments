def kwic(document): #this file puts the passed string into a list by splitting it by spaces (each word is one string in list)
	if document == "":
		document = []
	else:
		
	return document
def get_init_list(document):
        return document.split()
def get_original_list(document):

        document = [document]
	return document
