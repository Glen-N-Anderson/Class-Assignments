def kwic(document, ignoreWords, listpairs, setPeriodBreaks): #this file puts the passed string into a list
	if document == "":
		document = []
	
	else:
		document = [document]
		
	print document
	return document
def get_original_list(document):
	document = [document]
	return document
